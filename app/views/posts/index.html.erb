<div class="row">
  <% for post in @posts %>
    <div class="col-3">
      <h4><%= post["title"] %></h4>
      <img class="img-fluid" src="<%= post["image"] %>" class="w-full">
      <p><%= post["description"] %></p>
      <p><%= post["date"] %></p>
    </div>
  <% end %>
</div>