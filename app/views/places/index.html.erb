<div class="row">
  <% for place in @places %>
    <div class="col-3">
      <h4><%= place["name"] %></h4>
      <img class="img-fluid" src="<%= post["image"] %>" class="w-full">
      <p><%= post["body"] %></p>
    </div>
  <% end %>
</div>